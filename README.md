# Easy Face Swap — Cloud

Runs on a RunPod GPU. Pod boot script clones this repo and runs `setup.sh`, which installs system + Python deps and launches the Gradio app on port 7860.

Not meant to be run locally — uses `/workspace/*` paths.
